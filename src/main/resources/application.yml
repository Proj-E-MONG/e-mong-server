server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${DB_NAME}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      max-lifetime: 177000

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

application:
  jwt:
    secret-key: c7940a36f9b9a267b10ac0d422361a0848b43e1a0a56a07af3c58f02b645e2ec
    expiration: 86400000 # 1일
    refresh-expiration: 60480000 # 7일

google:
  sheets:
    credentials-path: classpath:e-mong-00edce0787f0.json # 인증 JSON 파일 경로
    spreadsheet-id: 10FNsydCT3EC9dqaul6zetyvc56sU2EOPvT5N2LXAnNA  # 스프레드시트 ID
    application-name: E-MONG       # 애플리케이션 이름
    data-range: Form Responses 1!B2:E           # 가져올 데이터 범위 (헤더 제외)

api:
  sign-up-url: http://localhost:8080/sign-up

twilio:
  account:
    sid: ${TWILIO_ACCOUNT_SID}
  auth:
    token: ${TWILIO_AUTH_TOKEN}
  phone:
    number: ${TWILIO_PHONE_NUMBER}